# log-wiki 依赖
# 安装: pip install -r requirements.txt
pyyaml>=6.0            # frontmatter 序列化(scripts/knowledge_base/ingest.py)
openai>=1.0           # OpenAI SDK,call_llm() 的结构化抽取
SQLAlchemy>=2.0       # MySQL 存储后端连接池、事务与 SQL 执行
PyMySQL>=1.1          # SQLAlchemy 的 mysql+pymysql 驱动;默认 SQLite 不连接 MySQL

# Web 服务(backend/server.py);纯命令行用 knowledge_base/ingest.py/query.py 时可不装
fastapi>=0.110
uvicorn>=0.27
